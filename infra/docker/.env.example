# =============================================================================
# Veelocity — Docker Environment Variables
# =============================================================================
# Copy to .env and set the required values. All app configuration (GitHub,
# Linear, Cursor, Greptile) is done in the app via Settings (gear icon).
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
POSTGRES_USER=veelocity
POSTGRES_PASSWORD=veelocity
POSTGRES_DB=veelocity
POSTGRES_PORT=5433

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
DEBUG=true
VEELOCITY_ENCRYPTION_KEY=

# Generate encryption key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# -----------------------------------------------------------------------------
# Production: public URL (baked into frontend at build time) + CORS
# -----------------------------------------------------------------------------
# VITE_API_BASE_URL=https://veelocity.tooling.veesion.io
# CORS_ALLOWED_ORIGINS=https://veelocity.tooling.veesion.io
