# infra/docker/.env.example
# Database
POSTGRES_USER=veelocity
POSTGRES_PASSWORD=veelocity
POSTGRES_DB=veelocity
POSTGRES_PORT=5433

# Application
DEBUG=true

# Encryption for credentials stored in DB (required to save API keys via Settings UI)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
VEELOCITY_ENCRYPTION_KEY=

# Connectors: configure GitHub & Linear in the app (Settings). Credentials are stored encrypted in the DB.
# Do not put API keys here; they are read from the database only.

# Sync settings
DEPLOYMENT_PATTERNS=deploy,release,publish

# Frontend (when running React app locally): copy frontend-react/.env.example to frontend-react/.env
# and set VITE_API_BASE_URL=http://localhost:8000 (or your backend URL).

# Alert notifications (optional)
# ALERT_WEBHOOK_URLS=https://your-server.com/webhook
# ALERT_EMAIL_TO=alerts@example.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=veelocity@example.com
