# Veelocity Frontend — Environment Variables
# Copy this file to .env before running the dev server.

# Backend API URL (must match where the backend is running)
VITE_API_BASE_URL=http://localhost:8000

# Sentry (optional — error reporting; self-hosted at https://sentry.tooling.veesion.io)
# DSN from Sentry: Project → Client Keys (DSN). Leave unset to disable.
# VITE_SENTRY_DSN=

# Sentry (optional — error reporting; self-hosted at https://sentry.tooling.veesion.io)
# DSN from Sentry: Project → Client Keys (DSN). Leave unset to disable.
# VITE_SENTRY_DSN=
