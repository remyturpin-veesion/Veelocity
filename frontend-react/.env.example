# Veelocity Frontend — Environment Variables
# Copy this file to .env before running the dev server.

# Backend API URL (must match where the backend is running)
VITE_API_BASE_URL=http://localhost:8000
